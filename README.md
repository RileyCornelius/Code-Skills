# Code Skills

## Skill Catalog

- `brainstorming` / `coding-brainstorming`: Turn coding ideas into approved designs and planning-ready implementation briefs before coding starts.
- `code-review` / `code-review`: Use when prompted to review or when finishing tasks or implementing major features.
- `debugging` / `debugging`: Use when encountering a bug, test failure, or unexpected behavior, before proposing fixes.
- `use-git-worktrees` / `use-git-worktrees`: Use when starting feature work that needs isolation from the current workspace.

## Install

### Install Global (Clone and Copy)


```bash
git clone https://github.com/rileycornelius/Code-Skills.git
cp -r rileycornelius/Code-Skills/skills/* .agents/skills/
```

## Install Claude Code (.claude)

```bash
git clone https://github.com/rileycornelius/Code-Skills.git
cp -r rileycornelius/Code-Skills/skills/* .claude/skills/
```

### Project Install (Clone and Copy) 

```bash
git clone https://github.com/rileycornelius/Code-Skills.git
mkdir -p .agents/skills
cp -R skills/* .agents/skills/
```
